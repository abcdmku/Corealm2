declare module 'virtual:corealm-release-navigation' {
  const release: { fingerprint:string; worldSeed:string; strategy:'solo'|'tiled'; sourceMeshes:number; sourceTriangles:number };
  export default release;
}
