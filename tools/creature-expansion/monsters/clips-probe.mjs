import fs from 'node:fs';import * as THREE from 'three';import {FBXLoader} from 'three/addons/loaders/FBXLoader.js';globalThis.window={URL:{createObjectURL:()=>''}};globalThis.document={createElementNS:()=>({addEventListener(){},removeEventListener(){},set src(v){},style:{}})};
const parse=p=>{const b=fs.readFileSync(p);return new FBXLoader().parse(b.buffer.slice(b.byteOffset,b.byteOffset+b.byteLength),'')};
const root=parse('test-results/creature-expansion/sources/monsters/cinder/Assets/Stylized3DMonster/Monster04/Monster04_AllAnim.fbx');for(const c of root.animations){const t=c.tracks.find(t=>t.name==='rootx.position'); console.log(c.name,t&&Array.from(t.values.slice(0,3)),t&&Array.from(t.values.slice(-3)));}
for(const name of ['idle','walk','run','HornAttack','GetHit','Die']){const r=parse(`test-results/creature-expansion/sources/monsters/basalt/Assets/FreeDragons/Animations/DragonBoar/${name}.fbx`);for(const c of r.animations){const t=c.tracks.find(t=>t.name==='Root.position'); console.log(name,c.name,c.duration,c.tracks.length,t&&Array.from(t.values.slice(0,3)),t&&Array.from(t.values.slice(-3)));}}
